<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="ProtoApp.Views.LoginPage"
             Shell.NavBarIsVisible="False">
    <!-- This is the prototype design for the login, whilst the login is visually present, and a database has been made it has not yet been connected. Due to the fact that the information stored within said database would belong to the client, and possibly children.
    -->
    <ContentPage.Content>
        <StackLayout Padding="10,0,10,0" VerticalOptions="Center">
            <Entry x:Name="Username" Text="Username" />
            <Entry x:Name="Password" Text="Password" />
            <Button VerticalOptions="Center" Text="Login" Command="{Binding LoginCommand}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>